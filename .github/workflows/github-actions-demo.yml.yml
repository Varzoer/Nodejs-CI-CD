name: CI

on:
  push:
  workflow_dispatch:

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Install software
        run: |
          # add commands to install the necessary software

      - name: checkout
        uses: actions/checkout@v3

      - name: install dependencies
        run: npm install
          
      - name: build project
        run: #build

      - name: Run unit tests
        run: npm test
